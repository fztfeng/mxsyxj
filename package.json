{
  "name": "moxi-watermark-camera",
  "version": "1.6.0",
  "description": "今日水印相机 - 支持实时定位修改、时间修改和防伪验真的水印相机应用",
  "main": "index.html",
  "scripts": {
    "build": "echo 'Static web app - no build step needed'",
    "cap:sync": "npx cap sync",
    "cap:add:android": "npx cap add android",
    "cap:copy": "npx cap copy",
    "android:build": "npx cap copy && cd android && ./gradlew assembleDebug",
    "android:release": "npx cap copy && cd android && ./gradlew assembleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/camera": "^6.0.0",
    "@capacitor/geolocation": "^6.0.0",
    "@capacitor/filesystem": "^6.0.0",
    "@capacitor/share": "^6.0.0",
    "@capacitor/app": "^6.0.0",
    "@capacitor/haptics": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  },
  "author": "今日水印相机",
  "license": "MIT"
}
